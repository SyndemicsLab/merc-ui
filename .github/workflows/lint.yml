name: Lint JavaScript/TypeScript

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  linter:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@1af3b93
      - uses: actions/setup-node@2028fbc
      - run: npm run lint
